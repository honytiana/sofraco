const nodeCron = require('node-cron');

const mailerCron = require('../cron/mailerCron');

module.exports = () => {
    // nodeCron.schedule('0 0 0 01 * *', () => {
    //     mailerCron();
    // });
};